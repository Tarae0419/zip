# 수강길잡이 개발 스프린트 플랜

| 항목 | 내용 |
| --- | --- |
| 기준 문서 | `docs/PRD.md` (v1.1), `CLAUDE.md` |
| 작성일 | 2026-07-29 |
| 최종 수정일 | 2026-07-30 |
| 상태 | Sprint 0~6, 8 완료 · Sprint 7 7.1만 보류(Neon×Vercel 연동, 사용자의 대시보드 조작 필요) |

---

## 문서 관리 방법

- 작업을 끝낼 때마다 해당 체크박스를 `- [ ]` → `- [x]`로 바꾼다. 완료 조건(DoD)을 모두 만족했을 때만 체크한다 — 코드가 있다고 체크하지 말고, 각 항목 하단의 **DoD**를 실제로 충족했는지 확인한다.
- 스프린트를 통째로 끝내면 위 표의 **상태** 줄과 아래 **진행 현황 요약**의 상태 컬럼을 함께 갱신하고, **최종 수정일**을 갱신한다.
- 스프린트는 순서대로 진행하는 것을 기본으로 한다. 앞 스프린트의 선행조건이 끝나지 않았는데 뒤 스프린트를 먼저 시작해야 하면, 그 사실과 이유를 해당 스프린트의 "메모" 항목에 남긴다.
- 진행 중 PRD에 없던 결정(예: 데이터 소스 변경, 범위 축소)을 내리면 하단 **오픈 이슈 / 의사결정 로그**에 날짜와 함께 기록한다. PRD 자체는 별도 합의 없이 고치지 않는다.
- 새 작업이 필요해지면 해당 스프린트 안에 항목을 추가하고, 기존 번호 체계를 건드리지 않도록 하위 항목(예: `1.4.1`)으로 끼워 넣는다.

---

## 진행 현황 요약

| Sprint | 범위 | PRD 매핑 | 상태 |
| --- | --- | --- | --- |
| 0 | 기반 인프라 (DB/배포/에이전트) | 10장 | ✅ 완료 |
| 1 | 익명 사용자 식별 & 리뷰 작성 | F1 쓰기 경로 | ✅ 완료 |
| 2 | AI 해시태그 추천 & 리뷰 요약 | F1 AI 경로 | ✅ 완료 |
| 3 | 분야 통합 검색 마무리 | F2 | ✅ 완료 |
| 4 | 산업/진로 분야 검색 | F3 | ✅ 완료 |
| 5 | 커리큘럼 데이터 확보 | F4 기반 | ✅ 완료 (더미 데이터 기반) |
| 6 | AI 커리큘럼 추천 엔진 | F4 | ✅ 완료 (커리큘럼 데이터 있는 2개 학과 한정, "과목 추가"·계절학기는 스코프 아웃) |
| 7 | 배포/운영 정비 & QA | 전체 | 🟡 7.1만 보류 (Neon×Vercel 대시보드 연동, 사용자 액션 필요), 나머지 완료 |
| 8 | PRD 범위 밖 추가 기능 (회원 시스템, F5, F1/F3/F4 고도화) | PRD에 없음 | ✅ 완료 |

PRD 11장 로드맵(Phase1=F1+F2, Phase2=F3, Phase3=F4)을 그대로 따르되, 실제 구현 단위(쓰기 경로/AI 경로/데이터 확보를 분리)로 더 잘게 쪼갰다.

---

## Sprint 0 — 기반 인프라 (완료)

**목표**: 이후 모든 스프린트가 딛고 설 인프라를 갖춘다.

- [x] 기술 스택 확정 및 아키텍처 문서화 (`docs/PRD.md` 10장)
- [x] Neon Postgres + Drizzle ORM 연결, `lib/db/schema.ts` 10개 테이블 설계·마이그레이션
- [x] 실제 개설 교과목 데이터 적재 (2026-1/2학기, 5,275건) — `lib/db/scripts/import-courses.ts`
- [x] Vercel 공식 스킬(`deploy-to-vercel` 등) + 프로젝트 전용 에이전트(`nextjs-frontend`/`neon-db`/`ai-integration`/`vercel-deploy`) 세팅
- [x] `CLAUDE.md` 작성
- [x] 홈/검색/과목상세 화면을 mock에서 실 DB 쿼리로 전환 (`lib/db/queries.ts`)

**메모**: F3(분야탐색)·F4(커리큘럼)는 아직 `lib/mock-data.ts`를 그대로 쓴다 — 각각 Sprint 4, 6에서 실 DB로 전환한다.

---

## Sprint 1 — 익명 사용자 식별 & 리뷰 작성 (F1 쓰기 경로)

**목표**: 지금은 조회만 되는 F1을 "쓰기"까지 완성해 실제 리뷰가 쌓이기 시작하게 한다. Sprint 2(AI 요약)와 Sprint 3(분야 태깅)이 여기서 나온 데이터에 의존한다.

**선행조건**: Sprint 0.

**작업 항목**

- [x] 1.1 익명 식별자 발급 — 로그인 없이 브라우저별 `anonId`를 쿠키로 발급하는 유틸/미들웨어 작성 (`users.anon_id`와 매칭)
  - DoD: 최초 방문 시 `users` 테이블에 anon 레코드가 생성되고, 재방문 시 동일 쿠키로 동일 레코드를 재사용한다.
  - 구현: `middleware.ts`(모든 요청에 `sgz_anon_id` httpOnly 쿠키 발급, 2년 유지) + `lib/auth/anon-user.ts`(`getAnonId`/`ensureAnonUser`, 리뷰 제출 시점에 upsert). curl로 `Set-Cookie` 발급 확인.
- [x] 1.2 리뷰 작성 Server Action — `components/review-composer.tsx`의 "실제 저장 로직 없음" 상태를 제거하고 `reviews` 테이블에 실제 insert
  - DoD: 별점(1~5)·본문·해시태그 다중선택이 저장되고, 과목 상세 페이지 새로고침 시 목록에 반영된다. (PRD 8.1 요구사항 1)
  - 구현: `lib/actions/reviews.ts`의 `submitReview`. `ReviewComposer`에 `courseId` prop 추가, 실제 서버 액션 호출 + 로딩/에러 상태로 교체.
  - **설계 메모**: `courses`는 학기·분반마다 별도 row(코드+분반+학기 유니크)라서, 리뷰를 정확히 보고 있던 `course.id`에 저장하되 **조회 시 같은 학수번호(code)의 모든 학기 row를 묶어 집계**하도록 `lib/db/queries.ts:getCourseView`를 수정했다(그렇지 않으면 학기가 바뀔 때마다 리뷰가 리셋된 것처럼 보임). 자세한 내용은 오픈 이슈 로그 참고.
- [x] 1.3 어뷰징 방지 최소 로직 — 동일 `anonId`의 동일 과목 단시간 반복 등록, 극단적 평점 연속 등록 등을 `is_filtered`로 걸러내는 최소 규칙
  - DoD: 반복 등록 시나리오를 재현했을 때 두 번째 리뷰부터 `is_filtered = true`로 저장되고 집계(평점/해시태그 %)에서 제외된다. (PRD 8.1 요구사항 7)
  - 구현: `lib/actions/reviews.ts:isRepeatSubmission` — 동일 anonId가 같은 과목(학수번호 기준)에 이미 리뷰가 있으면 이후 제출은 `is_filtered=true`로 저장(삭제하지 않고 감사 목적으로 보관). 실 DB에 테스트 데이터를 넣어 1번째 `isFiltered=false`, 2번째 `isFiltered=true`, 집계에서 2번째가 제외됨을 확인 후 정리.
  - 범위 밖으로 남긴 것: 극단적 평점(별점 테러) 자체를 탐지하는 통계 기반 로직은 아직 없음 — 리뷰 볼륨이 쌓인 뒤 Sprint 2~3 사이에 재검토.
- [x] 1.4 과목 상세 페이지 캐시/재검증 — 리뷰 작성 직후 `getCourseView` 결과가 갱신되도록 revalidate 처리
  - DoD: 리뷰 등록 후 페이지 새로고침 없이(또는 즉시 새로고침으로) 리뷰 수·평점이 바뀐다.
  - 구현: `submitReview`에서 `revalidatePath("/courses/[id]")` + `revalidatePath("/")` 호출, 클라이언트에서 성공 시 `router.refresh()`.
- [x] 1.5 홈 "인기 과목" 로직 재검토 — 리뷰가 실제로 쌓이기 시작하면 `enrolledCount` 대신/함께 리뷰 수·평점을 반영할지 결정
  - DoD: 결정 내용을 오픈 이슈 로그에 기록하고, 필요 시 `getPopularCourses` 정렬 기준 수정.
  - 결정: 이번 스프린트에서는 그대로 `enrolledCount` 순 유지 (아래 오픈 이슈 로그 참고). 실사용 리뷰가 충분히 쌓이면 재검토.

**메모(다음 세션에서 브라우저로 직접 확인할 것)**: DB insert·중복 필터링·집계 로직은 Neon에 테스트 리뷰를 직접 넣고 지운 뒤 검증했고, 미들웨어 쿠키 발급과 페이지 렌더링은 curl로 확인했다. 다만 이 세션에는 브라우저 자동화 도구가 없어 **모달 클릭 → 별점 선택 → 등록 버튼까지 실제 브라우저 인터랙션은 아직 육안으로 확인 못 했다** — 다음 작업 시작 전에 한 번 실제로 클릭해보는 걸 권장.

**담당 에이전트**: `nextjs-frontend`(폼/Server Action UI), `neon-db`(어뷰징 규칙·인덱스)

---

## Sprint 2 — AI 해시태그 추천 & 리뷰 요약 (F1 AI 경로)

**목표**: F1을 PRD 완료 조건까지 끝낸다.

**선행조건**: Sprint 1 (리뷰가 실제로 쌓여야 요약 대상이 생김). LLM 벤더 결정 필요(아래 오픈 이슈 참고).

**작업 항목**

- [x] 2.1 LLM API 벤더 결정 — 오픈 이슈 로그에 결정 기록, `ai-integration` 에이전트에 반영
  - 결정: **OpenAI** (`gpt-4o-mini`, `lib/ai/openai-client.ts`의 `AI_MODEL` 상수). 키는 `.env.local`의 `OPEN_AI_API_KEY`(표준 `OPENAI_API_KEY`가 아님 — SDK에 넘길 때 명시적으로 지정).
- [x] 2.2 자유 텍스트 → 해시태그 추천 — 리뷰 작성 중 입력한 본문을 바탕으로 사전 정의 태그 후보를 추천하는 API
  - DoD: `review-composer.tsx`에서 본문 입력 후 AI 추천 태그가 실제로 채워지고 사용자가 채택/수정할 수 있다. (PRD 8.1 요구사항 2)
  - 구현: `lib/ai/hashtags.ts:suggestHashtags` (본문 10자 미만이면 호출 안 함, 모델 응답을 `predefinedReviewTags` 화이트리스트로 서버에서 재검증). `lib/actions/reviews.ts:suggestReviewHashtags`로 노출, 컴포저에 "AI로 태그 추천받기" 버튼 추가(자동 실시간 호출이 아니라 명시적 클릭 — 비용/키입력 중 과호출 방지).
  - 실 API로 테스트: "과제가 정말 많고 시험도 어려웠지만 교수님은 친절"이라는 문장에 대해 `["과제많음", "시험어려움"]` 반환 확인.
- [x] 2.3 과목별 AI 요약 생성 — 리뷰 5개 이상인 과목을 대상으로 3~5문장 요약(전반 평가·장단점·추천 대상 포함) 생성 후 `summaries` 테이블에 캐싱
  - DoD: 리뷰 5개 이상 과목 상세 페이지에서 실제 생성된 요약이 노출되고, `app/courses/[id]/page.tsx`의 "AI 요약을 준비하고 있어요" 상태가 정상 동작으로 대체된다.
  - 구현: `lib/ai/summary.ts:generateCourseSummary`, `lib/actions/reviews.ts:maybeRegenerateSummary`에서 리뷰 등록 성공 시 호출. 실 DB에 테스트 리뷰 5건을 넣어 실제 OpenAI 요약이 생성·저장되고 `getCourseView`가 그 요약을 반환함을 확인 후 정리.
  - **설계 메모**: `summaries.courseId`는 학기별 row 중 하나를 가리켜야 하므로, "가장 최근 학기" row를 대표(canonical)로 고정한다(`getCanonicalCourseId`, `lib/db/queries.ts`). 대표가 바뀔 경우를 대비해 재생성 시 다른 형제 row에 붙은 예전 요약은 정리한다.
- [x] 2.4 요약 재생성 트리거 — 신규 리뷰가 일정 수 누적되면 요약을 자동 재생성
  - DoD: 재생성 기준(예: N개 누적)을 코드/문서에 명시하고, 기준 충족 시 `summaries.generated_at`이 갱신됨을 확인한다.
  - 결정: 최초 5개 도달 시 1차 생성, 이후 유효 리뷰가 3개 늘어날 때마다 재생성(`SUMMARY_REGEN_INCREMENT = 3`, `lib/actions/reviews.ts`). 매 리뷰마다 재생성하면 비용이 계속 나가므로 완충 구간을 뒀다. 리뷰 등록 Server Action 안에서 동기 처리 — 서버리스 요청 하나가 리뷰 저장+AI 호출을 함께 하므로, 향후 리뷰량이 많아지면 비동기/큐로 옮기는 걸 Sprint 7에서 재검토.
- [x] 2.5 극단적 리뷰 처리 — 평점이 크게 갈리는 과목은 요약에 "호불호가 갈리는 강의"를 명시하도록 프롬프트 반영 (PRD 8.1 Edge Case)
  - DoD: 의도적으로 평점이 양극화된 테스트 데이터로 생성했을 때 요약 문구에 반영됨을 확인한다.
  - 구현: 4~5점과 1~2점 비율이 전체의 60% 이상이면 `isPolarized`로 판단해 프롬프트에 지시 추가. 평점 5·2·4·5·1로 구성된 테스트 데이터로 실제 생성해 "호불호가 갈리는 평판"이라는 문구가 요약에 포함됨을 확인.

**메모(다음 세션에서 브라우저로 직접 확인할 것)**: "AI로 태그 추천받기" 버튼 클릭, AI 요약 카드가 실제 화면에 표시되는 모습은 이번에도 curl/직접 스크립트로만 검증했고 브라우저 클릭으로는 못 봤다(브라우저 자동화 도구 없음). 로직 자체는 실 OpenAI API + 실 DB로 검증 완료.

**담당 에이전트**: `ai-integration`

---

## Sprint 3 — 분야 통합 검색 마무리 (F2)

**목표**: 지금 "분야 일치" 검색은 `field_tags`가 비어 있어 항상 빈 결과다. 실제 태그 데이터를 채워 F2를 PRD 완료 조건까지 끝낸다.

**선행조건**: Sprint 0(courses 데이터). Sprint 2와 병행 가능(서로 독립).

**작업 항목**

- [x] 3.1 학문분야 분류 체계 정의 — 대분류-소분류 체계 설계 (PRD 8.2 요구사항 2), `field_tags.parent_id`로 표현
  - 구현: `lib/db/scripts/seed-field-tags.ts` — 실제 147개 개설학과 이름을 훑어 대분류 10개(인문학/사회과학/상경·경영/법학/자연과학/공학/의약·보건계열/농림수산·생명계열/예술·체육/교육학) × 소분류 66개, 총 76개 태그. 표준 분류표를 그대로 쓰지 않고 이 학교의 실제 학과 구성에 맞춰 직접 구성했다. `pnpm db:seed-field-tags`로 재실행 가능(이름 기준 upsert).
- [x] 3.2 동의어 사전 시딩 — `field_tags.synonyms`에 "수학"↔"수리과학" 같은 동의어 데이터 입력 (PRD 8.2 요구사항 6)
  - 구현: 소분류별로 흔히 쓰이는 별칭을 `synonyms`에 함께 시딩(예: 컴퓨터공학·소프트웨어 ↔ 전산학/컴퓨터과학/인공지능). **처음엔 검색 쿼리가 태그명만 보고 동의어 컬럼을 실제로 조회하지 않는 버그가 있었다** — `searchCoursesByFieldTag`(`lib/db/queries.ts`)에 `jsonb_array_elements_text`로 동의어를 함께 매칭하도록 수정 후 "수리과학" 검색 시 "수학" 태그가 달린 과목이 뜨는 것을 확인했다.
- [x] 3.3 과목-분야 AI 1차 분류 — 과목명을 기반으로 `course_field_tags` 자동 생성
  - DoD: 최소 1개 이상 학과의 과목이 실제로 분야 태깅되고, `/search?q=수학` 같은 쿼리에서 "분야 일치" 섹션에 실제 결과가 뜬다.
  - 구현: `lib/db/scripts/classify-course-fields.ts` — 같은 학수번호(또는 이름)를 공유하는 학기별 row를 하나의 "과목"으로 묶어 고유 과목 4,457개를 25개씩 배치(동시 8개)로 OpenAI에 분류 요청, 결과를 `field-classification-result.json`에 저장(DB에 바로 안 씀). 3,870개(86.8%) 과목에 태그가 부여됨 — 나머지는 이름만으로 분야를 특정하기 어려운 과목(세미나/실습 등)이라 빈 배열로 남음, 별도 조치는 하지 않음.
  - 강의계획서 기반 분류는 원본 데이터에 강의계획서가 없어(Sprint 0 CLAUDE.md 참고) 과목명만으로 진행했다.
  - 검증: `/search?q=수학` → "분야 일치: 수학" 섹션에 60건(상한) 노출 확인.
- [x] 3.4 담당자 검수 플로우 — AI 1차 분류 결과를 사람이 확인/수정할 수 있는 최소한의 관리 화면 또는 스크립트
  - DoD: 잘못 태깅된 항목을 수정하는 절차가 문서화되어 있다.
  - 절차: (1) `pnpm db:classify-fields`로 분류만 실행하고 `lib/db/scripts/field-classification-result.json`에 저장(DB 미반영). (2) 사람이 그 JSON을 열어 `tags` 배열을 직접 수정/삭제한다. (3) `pnpm db:apply-field-classification`으로 DB에 반영(`onConflictDoNothing`이라 재실행해도 안전). 이미 반영된 항목을 고칠 땐 `course_field_tags`에서 `courseId`+`fieldTagId` 조합을 지우고 다시 적용하거나 직접 insert한다. 이번 라운드는 무작위 표본(수학·음악·캡스톤 계열 등)만 육안 검수했고, 전수 검수는 하지 않았다.
- [x] 3.5 교양 과목 데이터 확보 — 현재 DB에는 학부전공 과목만 있고 교양 과목이 없다 (CLAUDE.md "Data notes" 참고). 별도 교양 개설과목 파일을 확보해 `courses`에 추가 적재
  - DoD: `requirement_type = '교양'` 행이 DB에 존재하고 검색/필터에서 정상 노출된다.
  - 구현(2026-07-30): 사용자가 `course/2026_1·2학기_교양.xlsx`(학부전공 파일과 동일 컬럼 구조)를 제공, 기존 학부전공 파일 2개도 `course/` 폴더로 옮겨 한 곳에 모았다. `import-courses.ts`의 `SOURCES`에 두 파일을 추가해 1,842건(고유 과목 361개) 적재. F2/F3가 새 교양 과목도 검색·연관도에서 잡도록 분류(`classify-course-fields`→`apply-field-classification`)·임베딩(`embed-courses`→`score-industry-relevance`→`apply-industry-relevance`) 파이프라인을 전체 재실행했다 — 두 스크립트 다 증분이 아니라 매번 전체를 다시 계산하는 구조라, 카탈로그가 또 바뀌면 같은 방식으로 재실행하면 된다.
  - 이 과정에서 `apply-industry-relevance.ts`가 형제 row(`getSiblingCourseIds`) 중복으로 "ON CONFLICT DO UPDATE cannot affect row a second time" 에러를 내는 버그를 발견 — 배치 삽입 전 `(courseId, industryTagId)` 기준 중복 제거(높은 점수 우선)를 추가해 고쳤다.
  - 검증: `/search?q=디지털리터러시` → "4차산업혁명과디지털리터러시"(교양) 검색 결과 노출 확인.

**메모(다음 세션에서 브라우저로 직접 확인할 것)**: `/search` 페이지에서 실제로 필터·정렬을 조작하며 "분야 일치" 섹션이 화면에 잘 나오는지는 curl로만 확인했다.

**담당 에이전트**: `ai-integration`(분류), `neon-db`(시딩/적재)

---

## Sprint 4 — 산업/진로 분야 검색 (F3)

**목표**: PRD Phase 2. `/fields` 페이지를 mock에서 실 DB로 전환한다.

**선행조건**: Sprint 3의 분야 태깅 파이프라인 경험을 재사용.

**작업 항목**

- [x] 4.1 산업/진로 태그셋 정의 — 반도체, AI·데이터사이언스 등 초기 카테고리를 `industry_tags`에 시딩 (PRD 8.3 요구사항 1)
  - 구현: `lib/db/scripts/seed-industry-tags.ts` — `lib/mock-data.ts`의 옛 `fieldCategories`와 동일한 6개(반도체/AI·데이터사이언스/바이오·헬스케어/금융·핀테크/콘텐츠·미디어/에너지·환경)로 시작, 아이콘도 그대로 재사용해 연속성 유지. `industry_tags`에 `description`·`icon`·`embedding` 컬럼 추가.
- [x] 4.2 연관도 스코어링 파이프라인 — 과목명 임베딩 + pgvector 유사도로 `course_industry_tags.relevance_score` 계산 (PRD 10.3)
  - DoD: 임의 산업 분야 하나에 대해 연관도 상위 과목 목록이 실제로 채워진다.
  - 구현: pgvector 0.8.1 확장 활성화(`enable-pgvector.ts`) → `course_embeddings` 테이블 추가(과목당 하나, `text-embedding-3-small`) → `embed-courses.ts`로 고유 과목 4,457개 임베딩 생성 → `score-industry-relevance.ts`가 코사인 유사도(`<=>` 연산자)로 태그당 상위 40개를 뽑아 검수용 JSON으로 저장.
  - **설계 메모**: 강의계획서가 없어(Sprint 0) 과목명만 임베딩했다. 산업 태그 쪽은 이름만으로는 임베딩이 부실해서 짧은 설명 + 키워드 목록을 붙여 임베딩했다(`industryTags.description`/시드 스크립트의 `keywords`). 최종 스코어는 매 요청마다 실시간으로 pgvector를 조회하지 않고 `course_industry_tags.relevance_score`에 캐싱 — pgvector는 이 오프라인 스코어링 단계에서만 쓰인다.
- [x] 4.3 담당자 검수 반영 — 3.4와 동일한 방식으로 산업 태그 결과 검수
  - 임계값(코사인 유사도) 0.3으로 처음 돌렸더니 "금속재료분석학"이 금융·핀테크로 잡히는 등 명백한 오탐이 섞여 0.4로 올림(6개 태그 합계 236건→167건). 그 후에도 남아있던 오탐(예: "디지털셰익스피어"가 바이오·헬스케어로 분류) 5건을 육안으로 찾아 JSON에서 직접 제거한 뒤 적용(`apply-industry-relevance.ts`, 최종 197건, 학기 fan-out 포함).
- [x] 4.4 `/fields` 페이지 실 DB 전환 — `components/fields-explorer.tsx`의 `fieldCategories`/mock 의존 제거, `lib/db/queries.ts`에 `getIndustryFields`/`getIndustryFieldCourses` 추가
  - DoD: 화면에서 카테고리별 과목 수·목록이 실제 DB 값과 일치한다.
  - `lib/mock-data.ts`에서 이제 아무 화면도 안 쓰는 `mockCourses`/`getCourseById`/`mockReviews`/`getReviewsByCourseId`/`fieldCategories`/`FieldCategory`/`aiSuggestedTags`를 함께 정리했다(F1/F2/F3는 완전히 실 DB로 전환됨 — 남은 mock은 F4용 `departments`/`interestFields`/`mockCurriculum`뿐).
- [x] 4.5 내 전공 vs 타 전공 구분 — 사용자의 학과와 비교해 "내 전공 과목"/"타 전공 과목" 라벨 표시 (PRD 8.3 요구사항 5)
  - DoD: 사용자의 학과 정보가 있을 때 라벨이 정확히 구분되고, 없을 때는 라벨을 생략한다.
  - 구현: `/fields`에 실제 개설학과 드롭다운(`components/my-department-select.tsx`) 추가, 선택 시 `lib/actions/user-profile.ts:setMyDepartment`가 `users.department`에 저장. `course_department_tracks`(자유 텍스트 라벨) 대신 `courses.department`(개설학과) 문자열을 그대로 비교 — 전자는 원본 표기가 학과명과 정확히 일치하지 않는 경우가 많아 더 불안정하다고 판단.
  - 학과 미설정 상태에서는 `ownMajorLabel`이 `undefined`라 라벨이 아예 안 뜨는 것을 확인했고, 테스트 학과를 넣었을 때 정확히 구분되는 것도 DB 스크립트로 확인.

**메모(다음 세션에서 브라우저로 직접 확인할 것)**: 아코디언 펼치기, 학과 드롭다운 선택 시 라벨이 바뀌는 모습은 curl/스크립트로만 검증했다.

**담당 에이전트**: `ai-integration`, `nextjs-frontend`

---

## Sprint 5 — 커리큘럼 데이터 확보 (F4 기반)

**목표**: F4는 4개 기능 중 복잡도가 가장 높고, 무엇보다 **데이터가 없으면 시작할 수 없다** (PRD 11장). 로직을 짜기 전에 데이터부터 확보한다.

**선행조건**: 없음(다른 스프린트와 독립적으로 먼저 시작 가능하지만, 데이터 확보에 시간이 걸릴 수 있어 최대한 일찍 착수 권장).

**작업 항목**

- [x] 5.1 학과 졸업요건 데이터 확보 방안 결정 — 학교 협의(교무처·학과 사무실) 또는 PRD 8.4에 명시된 더미 데이터 삽입 중 선택
  - DoD: 결정 내용과 근거를 오픈 이슈 로그에 기록.
  - 결정: **더미 데이터** (PRD 8.4가 "더미 데이터 인서트해서 할 수 있도록 추가"를 명시적으로 허용). 학교 협의는 이 스프린트 범위 밖.
- [x] 5.2 `curricula` 테이블 시딩 — 학과별 전공필수 목록, 전공선택 최소학점, 교양 요건, 총 이수학점을 최소 1개 학과 기준으로 입력
  - DoD: 최소 1개 (학과, 입학년도) 조합에 대해 조회 가능한 실데이터가 있다.
  - 구현: `lib/db/scripts/seed-curricula.ts` — 전자공학부·컴퓨터인공지능학부 2개 학과, 2024학번 기준으로 시딩. **`requiredCourseCodes`(전공필수 목록)는 실제 개설 데이터에서 그대로 가져온 진짜 학수번호**(예: 컴퓨터인공지능학부 UCAI003 알고리즘 등 6개)이고, `electiveMinCredits`/`generalEducationRequirement`/`totalCreditsRequired`만 통상적인 공학계열 수치를 참고한 더미값(총 130학점, 전공선택 최소 45학점 등)이다. `pnpm db:seed-curricula`로 재실행 가능(학과+입학년도 기준 upsert).
- [x] 5.3 선수과목 관계 데이터 확보 — 현재 `courses.prerequisite_codes`는 항상 비어 있음(원본 엑셀에 없음). 별도 소스 확보 또는 수동 입력 범위 결정
  - DoD: 최소 1개 학과 커리큘럼에 대해 선수과목 순서가 실제로 채워진다. 확보 불가 시 "F4는 선수과목 순서를 반영하지 않는다"를 명시적 제약으로 문서화.
  - 결정: 공식 교육과정표를 확보하지 못했지만, 5.1과 같은 더미 데이터 허용 원칙을 적용해 일반적인 전공 이수 상식(예: "알고리즘"은 "자료구조" 이후, "전자회로실험"은 "회로이론 1"·"기초회로실험" 이후)으로 두 학과에 6개 관계를 채웠다(`seed-curricula.ts`의 `PREREQUISITES`). **실제 학과 교육과정표로 검증된 값이 아니므로**, F4 UI에서 선수과목 순서를 보여줄 때 "예시/참고용"임을 명시해야 한다 — 이 제약을 F4 UI 설계(Sprint 6)에 반영할 것.
- [x] 5.4 커리큘럼 버전 관리 확인 — (학과, 입학년도) 유니크 제약이 실제 다년도 데이터로 문제없이 동작하는지 검증
  - 전자공학부에 2023학번 버전을 임시로 추가해 2024학번과 공존하는 것을 확인한 뒤 테스트 데이터를 정리했다. `onConflictDoUpdate` 기준(department+admissionYear)도 재실행으로 확인.

**메모**: 이번 스프린트는 배포 환경 설정(Vercel CLI 연결, env 변수 등록, 첫 배포)도 사용자 요청으로 중간에 진행했다 — 자세한 내용은 대화 기록 참고, 이 문서에는 F4 관련 항목만 기록한다.

**담당 에이전트**: `neon-db`

---

## Sprint 6 — AI 커리큘럼 추천 엔진 (F4)

**목표**: PRD Phase 3. `/curriculum`을 mock에서 실 추천 로직으로 전환한다.

**선행조건**: Sprint 5(커리큘럼 데이터), Sprint 4(관심분야 연관도 데이터), Sprint 1(사용자 기이수 과목 입력 경로).

**작업 항목**

- [x] 6.1 입력 폼 실데이터 연동 — 학과(복수전공 포함)·현재 학년/학기·기이수 과목·관심분야·잔여 학기 입력을 `users` 테이블에 저장 (PRD 8.4 입력 1~4)
  - 구현: `components/curriculum-planner.tsx` 전면 재작성 — 학과·복수전공은 `curricula`가 있는 학과만(현재 2개) 선택 가능, 기이수 전공필수는 해당 학과의 실제 전공필수 목록에서 체크박스로 선택, 관심분야는 `industry_tags`(Sprint 4)에서 클릭 순서 = 우선순위로 다중 선택. `lib/actions/curriculum.ts:generateCurriculumPlan`이 매 생성 시 `users` 테이블에 그대로 저장.
- [x] 6.2 결정론적 학점 계산 로직 — 미이수 전공필수 우선 배치, 전공선택 잔여 학점 계산 (PRD 8.4 추천로직 6~7). **AI 위임 대상이 아님** — 일반 백엔드 로직으로 구현
  - DoD: 임의 사용자 입력에 대해 전공필수 미이수 과목이 우선 배치된 학기별 결과가 나온다.
  - 구현: `lib/curriculum/plan.ts` (AI 미사용, 순수 TS). 선수과목이 있는 과목은 그 선수과목이 배치된 학기 다음부터 배치되도록 간이 위상정렬을 적용. 학기당 최대 18학점, 목표 16학점.
  - 검증: 컴퓨터인공지능학부로 직접 테스트 — "자료구조"를 이수 완료로 표시했더니 "자료구조"가 선수과목인 "알고리즘"이 1학기에, 그 "알고리즘"을 선수과목으로 하는 "인공지능"이 정확히 2학기로 밀려 배치되는 것을 확인.
- [x] 6.3 관심분야 매칭 추천 (AI 위임 부분) — 전공선택 잔여·자유선택·교양 학점에 대해 관심분야 연관도가 높은 과목을 추천하고 사유 생성 (PRD 8.4 추천로직 8)
  - 구현: 과목 선정 자체는 Sprint 4에서 이미 계산해 둔 `course_industry_tags.relevance_score`로 결정론적으로 정렬(본인 전공 우선, 그다음 연관도순 — PRD 8.4 요구사항 8과 동일한 원칙). **PRD 10.3이 명시한 "관심분야 매칭 추천 부분만 LLM API에 위임"**은 이 선정 결과에 대한 한 줄 추천 사유 문장 생성으로 한정해서 구현했다(`lib/ai/curriculum-reasons.ts`). AI 실패 시 결정론적 템플릿 문구로 자동 대체.
  - 실 데이터로 테스트: 컴퓨터인공지능학부 + AI·데이터사이언스 관심분야로 생성 → 연관도 0.53(타 전공)보다 0.47(본전공, 데이터마이닝)이 실제로 더 상위에 배치됨을 확인. AI가 각 과목마다 구체적인(뭉뚱그리지 않은) 한국어 추천 사유를 정상 생성.
- [x] 6.4 인터랙티브 재계산 — 사용자가 과목을 제외/추가하면 나머지 추천이 재계산 (PRD 8.4 요구사항 12)
  - DoD: `components/curriculum-planner.tsx`의 `setTimeout` 목업 로직이 실제 서버 재계산 호출로 대체된다.
  - 구현: 과목별 "제외" 버튼 → `excludedCourseCodes`에 누적하고 `generateCurriculumPlan`을 전체 재호출(같은 입력 + 갱신된 제외 목록)해 처음부터 다시 계산. 제외된 필수과목은 다음 계산부터 후보에서 빠지고, 빈 자리는 관심분야 후보로 채워진다.
  - **스코프 축소**: "과목 추가"(목록에 없는 과목을 사용자가 직접 검색해 끼워 넣는 것)는 이번 스프린트에서 구현하지 않았다 — 기존 mock UI에도 없던 기능이라 별도 과목 검색 UI가 새로 필요해 범위를 넘어선다고 판단.
- [x] 6.5 서버리스 타임아웃 대응 — 연산이 길어질 경우 스트리밍 응답 또는 상태 폴링 구조 적용 (PRD 10.4)
  - **결정**: 현재 규모(학과 2개, 관심분야 6개, 후보 최대 수십 개, OpenAI 호출 1회)에서는 전체 생성이 수 초 내로 끝나 스트리밍/폴링이 불필요하다고 판단해 구현하지 않았다. 학과·후보 규모가 커지면 재검토(Sprint 7 후보).
- [x] 6.6 참고용 안내 문구 — "최종 확인은 학과 사무실을 통해주세요" 문구가 실제 결과 화면에도 노출되는지 확인 (PRD 8.4 완료조건)
  - 매 생성 결과에 "본 추천은 참고용이며, 최종 확인은 학과 사무실을 통해주세요"와 "선수과목 순서는 공식 교육과정표가 아니라 예시 데이터 기준"(Sprint 5 더미 데이터 고지) 두 문구를 항상 함께 노출한다.
- [x] 6.7 복수전공/부전공, 고학년 엣지케이스 처리 (PRD 8.4 Edge Case)
  - 복수전공: 커리큘럼 데이터가 있는 다른 학과를 선택하면 그 학과의 미이수 전공필수도 "복수전공필수"로 함께 배치. 커리큘럼 데이터가 없는 학과를 고르면(현재는 2개뿐이라 선택지 자체가 그 안에서만 제공되므로 사실상 발생 안 함, 그래도 방어 코드는 있음) 안내 문구로 대체.
  - 고학년(추천 여지 적음): 미이수 전공필수가 없고 총 잔여 필요학점이 6학점 이하면 "이미 졸업 요건의 대부분을 이수하셨어요" 안내를 노출.
  - **미구현**: 계절학기까지 탐색 범위 확장(PRD Edge Case 3번째 항목)은 계절학기 개설 데이터 자체가 없어 구현하지 않았다 — 관심분야 후보가 부족하면 대신 "교양·타 전공 과목을 직접 살펴보시라"는 안내만 표시.

**메모(다음 세션에서 브라우저로 직접 확인할 것)**: 폼 입력 → 학기 탭 전환 → 과목 제외 클릭 같은 실제 인터랙션은 이번에도 curl과 로직 재현 스크립트로만 검증했다. `/curriculum` 페이지 자체는 curl로 200과 폼 렌더링(기본 선택된 학과의 전공필수 체크박스까지)을 확인했지만, 브라우저 클릭 흐름은 못 봤다.

**담당 에이전트**: `ai-integration`(추천), `nextjs-frontend`(UI), `neon-db`(학점 계산 쿼리)

---

## Sprint 7 — 배포/운영 정비 & QA

**목표**: 실사용 가능한 수준으로 다듬는다. 특정 Phase에 종속되지 않으므로 앞 스프린트들과 일부 병행 가능.

**작업 항목**

- [ ] 7.1 Neon 브랜치 × Vercel 프리뷰 연동 — `.claude/skills/neon-branch-preview-sync/SKILL.md` 절차를 실제로 Vercel 대시보드에서 설정
  - **블로킹, 사용자 액션 필요**: Vercel의 Neon 통합(`vercel integration add neon`)은 OAuth 동의·브라우저 콘솔 조작이 필요해 CLI로 대신 실행할 수 없다(로그인과 동일한 제약). Vercel 대시보드 → 프로젝트 `zip` → Storage 탭 → Neon 연결에서 **기존 Neon 계정/프로젝트를 연결**하고(새 프로젝트 생성 아님), "브랜치별 프리뷰 DB 생성" 옵션을 켜야 한다. 완료 후 알려주면 검증하겠다.
- [x] 7.2 접근성/UX 감사 — `web-design-guidelines` 스킬로 주요 화면(리뷰 작성 폼, 검색 필터, 커리큘럼 플래너) 점검 및 수정
  - 발견 7건 모두 수정: 리뷰 모달에 `overscroll-behavior: contain` 추가, 별점 버튼 그룹에 `role="group"`+`aria-label`+`aria-pressed`, 정렬/관심분야 토글 버튼에 `aria-pressed`, "..." → "…" 3곳, 커리큘럼 플래너에 제외 과목 되돌리기 버튼 추가(제외가 되돌릴 수단 없이 즉시 반영되던 문제).
- [x] 7.3 Vercel 비용/성능 점검 — `vercel-optimize` 스킬로 F4 등 무거운 라우트 점검
  - Observability Plus가 이 팀에 비활성화돼 있어(`no_oplus_probe`) 라우트별 실측 지표 기반 감사는 불가 — 사용자에게 확인 후 스캐너 전용(코드 패턴만 검사) 모드로 진행하기로 결정. 코드 스캐너 자체가 이 저장소 구조(`pnpm-workspace.yaml`은 있지만 워크스페이스 패키지가 0개)를 모노레포로 오인해 라우트 매핑에 실패(0 routes, 21,860개 파일 스캔 — `node_modules` 포함된 것으로 추정)해 유효한 결과를 못 냈다. 대신 직접 코드 리뷰로 진행: 트래픽·데이터 규모가 작아(학과 2개, 관심분야 6개, 생성당 OpenAI 호출 1회) 지금 단계에서 손볼 만한 뚜렷한 비효율은 없다고 판단. 실 트래픽이 쌓이면 Observability Plus를 켜고 재감사할 것.
- [x] 7.4 프로덕션 배포 — `deploy-to-vercel`/`vercel-cli-with-tokens` 스킬로 첫 프로덕션 배포 (사용자 명시적 승인 필요)
  - **이미 완료된 상태로 확인됨**: 이 저장소가 `main` 브랜치를 Vercel의 프로덕션 브랜치로 인식해서, Sprint 4 연결 이후 `main`에 푸시할 때마다 자동으로 프로덕션 배포가 실행되고 있었다(별도 `--prod` 지시 없이). 현재 프로덕션(`https://zip-tarae0419s-projects.vercel.app` 등 별칭)은 Sprint 6까지 반영된 상태로 Ready 확인.
  - **운영상 주의점**: 현재 `main` 푸시 = 즉시 프로덕션 배포라 프리뷰/스테이징 단계가 없다. 7.1(Neon 프리뷰 브랜치 연동)과 별개로, 기능 브랜치 + PR 기반 워크플로로 바꾸는 걸 고려할 만하다 — 지금 결정하지 않고 기록만 남긴다.
- [x] 7.5 최소 테스트 체계 도입 — 현재 테스트 스위트 없음(CLAUDE.md 참고). 핵심 쿼리(`lib/db/queries.ts`)와 학점 계산 로직(6.2) 최소 단위 테스트 추가
  - Vitest 도입(`vitest.config.ts`, `pnpm test`). `lib/curriculum/plan.test.ts` — 순수 로직 단위테스트 10개(선수과목 순서 배치, 학기당 학점 상한, 관심분야 예산 소진, 본인전공 아닐 때만 문구 추가 등). `lib/db/queries.integration.test.ts` — 실 Neon DB 대상 읽기 전용 통합 스모크 테스트 5개(`DATABASE_URL` 없으면 자동 스킵). 총 15개 전부 통과.
  - **부수 발견**: QA 중 `next build`가 "middleware 컨벤션은 deprecated, proxy 사용" 경고를 냄(Next.js 16). `middleware.ts` → `proxy.ts`로 마이그레이션(`export function middleware` → `export function proxy`, `lib/auth/anon-user.ts`의 import 경로 수정). 빌드 경고 해소, 쿠키 발급 동작은 그대로 확인.

---

## Sprint 8 — PRD 범위 밖 추가 기능 (완료)

**목표**: F1~F4가 자리잡은 뒤 실사용 흐름에서 사용자가 요청한, PRD v1.1에는 아예 없던 기능들. 번호 체계는 앞 스프린트와 독립적으로 8.N을 새로 쓴다.

**작업 항목**

- [x] 8.1 회원가입/로그인 도입 — PRD는 "개인정보 최소 수집" 원칙에 따라 익명 전용을 전제하지만, 실제 계정(학번+비밀번호) 기반으로 전환
  - 구현: `app/signup`, `app/login`, `lib/actions/auth.ts` (`requestSignup`/`verifySignupCode`/`login`/`logout`), `proxy.ts`의 로그인 가드. `@jbnu.ac.kr` 이메일 인증(6자리 코드, `email_verifications` 테이블). 기존 anonId를 로그인 세션 토큰으로 그대로 재사용해 리뷰·커리큘럼 프로필 등 anonId 기반 로직은 손대지 않았다.
  - 기존 익명 `users` row(학번·비밀번호 없음)는 로그인 불가 — 마이그레이션 경로 없음, 의도된 스코프 축소(채팅 기록에서만 합의됐던 걸 이 문서에 처음 기록).
- [x] 8.2 F5 — 시간표 & 캠퍼스 이동동선 (PRD 범위 밖)
  - 장바구니(`/cart`)에 담은 과목으로 주간 시간표 자동 생성, 담기 시점에 요일·시간 겹침을 감지해 차단 (`lib/timetable/schedule.ts`의 `findTimeConflict`)
  - 카카오맵 JS SDK + 카카오 로컬 API로 실제 지도 기반 이동동선 시각화. 건물명 67개를 지오코딩해 `lib/timetable/building-coordinates.json`에 저장(`pnpm db:geocode-buildings`), 좌표 못 찾은 소수 건물은 기존 스케매틱(해시 기반 추정) 배치로 폴백
  - 홈 "인기 과목" 로직을 `enrolledCount` 기준에서 최근 30일 리뷰 증가량 + 평균 평점 3.5 이상 기준으로 변경(1.5/4.5의 "재검토" 약속을 실제로 이행), 로그인 사용자의 소속 학과로 범위를 좁힘
- [x] 8.3 F4 고도화 — 학년 적합도 반영
  - `course_department_tracks`의 최소 권장 학년을 확인해, 학생이 아직 도달하지 않은 학년(1개 학년 선이수는 허용, `GRADE_LOOKAHEAD=1`)의 과목을 이른 학기에 추천하지 않음
  - 전공선택/관심분야 추천 배치를 학기별 2패스(그 학년에 맞는 과목 우선 → 남으면 선이수)로 바꿔, 특정 학기에 몰리지 않고 전체 학기에 고르게 분산되도록 함
  - 커리큘럼 설계 폼에서 "학과" 직접 선택을 없애고 회원가입 때 저장된 본인 학과를 고정 사용, "복수전공/부전공"은 F4 지원 2개 학과가 아니라 실제 전체 학과 목록에서 선택 가능하도록 확장
  - "현재 학기"(1/2학기) 입력 추가 — 학기 라벨이 "N학년 M학기" 형식으로 정확히 계산되도록
- [x] 8.4 F3 확장 — 산업분야 카테고리 6개 → 11개
  - 모빌리티·로보틱스 / 스마트시티·건설 / 교육·에듀테크 / 농식품·스마트팜 / 물류·유통 추가. 기존 임베딩·연관도 스코어링 파이프라인을 그대로 재사용(`seed-industry-tags` → `score-industry-relevance` → `apply-industry-relevance`)
- [x] 8.5 F1 — 본인 수강평 삭제 기능
  - `lib/actions/reviews.ts`의 `deleteReview` — 클라이언트가 보낸 값을 믿지 않고, 리뷰의 `authorAnonId`가 현재 세션 쿠키의 anonId와 일치하는지 서버에서 직접 확인한 뒤에만 삭제
- [x] 8.6 F2 — 교양(gen-ed) 과목 카탈로그 추가 = Sprint 3.5 완료 처리 (상세는 위 3.5 항목 참고)
- [x] 8.7 F2 — 일반선택/교직/군사학 카탈로그 추가, 학수번호 없는 과목 재수입 시 중복 삽입되던 버그 발견·수정
  - `requirementTypeEnum`에 `일반선택`/`교직`/`군사학` 3개 값 추가(마이그레이션 `0004_medical_agent_zero.sql`, 순수 `ALTER TYPE ... ADD VALUE`라 안전), `Requirement`/`requirementAccentColor`/`requirementStyles`/검색 필터 옵션 등 8개 값을 다루는 곳 전부 갱신
  - **버그 발견**: `courses_code_section_semester_idx`가 `code`가 NULL인 row끼리는 서로 다르다고 취급해(Postgres 유니크 인덱스의 NULL 시맨틱), `import-courses.ts`를 재실행할 때마다 학수번호 없는 과목(교양·일반선택 다수, 학부전공 일부)이 계속 새 row로 중복 삽입되고 있었다 — 이번 세션 이전의 과거 재실행분까지 포함해 총 1302개 그룹, 2341건의 중복 row가 쌓여 있었다. 리뷰가 갈라진 사례는 없었지만(확인함), 검색 결과에 같은 과목이 여러 번 뜨는 문제로 이어질 수 있었다.
  - **수정**: `insertOne`에서 `code`가 없는 행은 `onConflictDoNothing` 대신 `(교과목명, 개설학과, 분반, 학기)`로 먼저 존재 여부를 직접 확인하도록 변경 — 재실행해도 이제 진짜로 0건 삽입됨을 확인.
  - 일반선택/군사학 카탈로그는 일부 row의 "학과" 컬럼이 통째로 비어 있음(원본 데이터 결측) — 군사학은 전 row가 예외 없이 "교무처 학사지원과" 소속이라 그 값으로 안전하게 채워 넣었고, 일반선택은 학과가 원예학과·약학과·체육교육과 등으로 다양해 추론할 수 없어 그 row들은 그대로 스킵(형식오류로 카운트)했다.
- [x] 8.8 F2/F3/F4 — 이수구분 배지를 "뷰어 학과 기준"으로 보정
  - 문제 제기: "전공필수"/"전공선택"/"기초필수"/"계열공통"은 그 과목을 개설한 학과의 커리큘럼에서만 의미가 성립하는데, 카드에는 뷰어가 어느 학과든 상관없이 원본 카탈로그 값이 그대로 떴다 — 타 학과 학생이 보면 "전공필수"가 실제로는 그냥 일반선택으로만 들을 수 있는 과목인데 오해를 살 수 있음.
  - 구현: `components/course-badges.tsx`에 `resolveDisplayRequirement(requirement, courseDepartment, viewerDepartment)` 추가 — 학과 종속적 4종(전공필수/전공선택/기초필수/계열공통)만 대상으로, 뷰어 학과를 알고 있고 과목의 개설학과와 다르면 "일반선택"으로 보정한다. 교양/일반선택/교직/군사학은 원래 학과 무관이라 그대로 둔다. 뷰어 학과를 모르면(로그인했지만 아직 학과 미설정) 원본 값 유지.
  - `CourseCard`에 `viewerDepartment` prop 추가(배지·상단 accent bar 둘 다 보정된 값 사용), 과목 상세 페이지도 동일 로직 적용. 홈/검색/분야탐색/장바구니 과목추가 화면까지 전부 뷰어의 학과를 조회해 내려주도록 연결.
  - **의도적으로 안 바꾼 것**: 검색 페이지의 "이수구분" 필터 자체는 원본 카탈로그 값 기준으로 그대로 둔다(필터까지 뷰어 학과별로 바꾸면 "이 필터로 검색하면 무슨 뜻인지"가 사람마다 달라져 더 헷갈릴 수 있어서) — 배지 표시만 보정한다.
- [x] 8.9 회원가입 "이름" 필드 추가 + 헤더 인사말
  - `users`/`email_verifications`에 `name` 컬럼 추가(마이그레이션 `0005_smooth_jane_foster.sql`). `email_verifications.name`은 `NOT NULL`이라, 적용 전 이미 소비됐거나 만료된 대기 행 1건을 정리하고 마이그레이션을 적용했다(진행 중이던 미인증 가입이 있었다면 코드 재요청이 필요했을 상황).
  - `requestSignup`이 `name`을 받아 검증(빈 값·50자 초과 거부)하고 `email_verifications`에 저장, `verifySignupCode`가 인증 완료 시 `users.name`으로 그대로 복사.
  - `components/signup-form.tsx`: 학번과 한 줄에 "이름" 입력을 추가하고, 학과 선택은 별도 전체 너비 행으로 이동.
  - `components/app-header.tsx`를 async 서버 컴포넌트로 바꿔 `getAnonId`+`getUserName`(신규, `lib/db/queries.ts`)을 조회, `SiteHeader`에 `userName` prop으로 전달 — 로그아웃 버튼 왼쪽에 "OOO님" 인사말로 노출. 과거(이름 없이 가입한) 계정은 `name`이 null이라 인사말 없이 로그아웃 버튼만 그대로 보임.
- [x] 8.10 비로그인 방문자용 랜딩 페이지 추가
  - `app/welcome/page.tsx` — DB 조회 없는 순수 정적 마케팅 페이지(F1~F4 요약 카드 + 로그인/회원가입 CTA), `/welcome`로 정적 프리렌더.
  - `proxy.ts`: `/welcome`을 `PUBLIC_PATHS`에 추가하고, 비로그인 사용자가 보호된 경로(`/` 등)에 접근했을 때의 리다이렉트 대상을 기존 `/login`에서 `/welcome`로 변경 — `/login`·`/signup`은 그대로 직접 접근 가능(북마크·즐겨찾기 호환).

**담당 에이전트**: `nextjs-frontend`(화면), `neon-db`(스키마/시딩), `ai-integration`(임베딩·분류 재실행), `vercel-deploy`(배포)

---

## Sprint 9 — 관리자 페이지 (계획, 미착수)

**목표**: `docs/PRD.md` 13장에 정리한 관리자(운영) 기능 요구사항을 실제로 구현한다. PRD 13장이 이 스프린트의
요구사항 원본이며, 착수 전 정리 단계에서만 작성됐다 — 구현은 아직 시작하지 않았다.

**작업 항목** (PRD 13.x와 번호 대응)

- [ ] 9.1 관리자 인증/권한 (PRD 13.2) — `users.role` 컬럼 추가, `/admin` 전용 라우트 가드
- [ ] 9.2 리뷰 모더레이션 (PRD 13.3) — 전체 리뷰 검색/조회, 수동 숨김·복원·강제 삭제
- [ ] 9.3 AI 생성 콘텐츠 검수 (PRD 13.4) — AI 요약 수정/재생성, 학문분야·산업분야 태그 검수 큐(현재
      `classify-course-fields.ts`/`score-industry-relevance.ts`의 파일 기반 검수를 화면으로 대체)
- [ ] 9.4 커리큘럼(F4) 데이터 관리 (PRD 13.5) — 학과별 `curricula` row·선수과목 CRUD, 더미/확정 데이터 구분 표시
- [ ] 9.5 과목 카탈로그 운영 (PRD 13.6) — 과목 노출 여부(`isPublic`) 토글, 재수입 이력 조회
- [ ] 9.6 사용자 관리 (PRD 13.7) — 계정 검색(마스킹), 정지/삭제, 관리자 권한 부여
- [ ] 9.7 운영 대시보드 (PRD 13.8)

**담당 에이전트**: `nextjs-frontend`(화면), `neon-db`(`role` 컬럼·스키마 변경)

---

## 오픈 이슈 / 의사결정 로그

> PRD 10.4는 ORM·LLM 벤더 같은 미결정 사항을 "14장 오픈 이슈"에서 관리한다고 적혀 있지만, 실제 `docs/PRD.md`에는 14장이 존재하지 않는다(12장에서 끝남). PRD를 임의로 고치는 대신, 이 문서에서 대신 추적한다.

| 날짜 | 이슈 | 결정 | 비고 |
| --- | --- | --- | --- |
| 2026-07-29 | ORM 선택 | **Drizzle** | Neon 서버리스 드라이버와의 통합, 콜드스타트/번들 크기 고려 |
| 2026-07-29 | LLM API 벤더 | **OpenAI** (`gpt-4o-mini`) | Sprint 2에서 실제 연동 완료. 키는 `.env.local`의 `OPEN_AI_API_KEY`(표준 이름 `OPENAI_API_KEY`가 아니라는 점 주의, `lib/ai/openai-client.ts`에서 명시적으로 읽음) |
| 2026-07-29 | AI 요약 재생성 주기 | 최초 5건, 이후 3건마다 재생성 | 리뷰마다 재생성하면 비용이 계속 나감. 서버리스 요청 안에서 동기 실행 — 리뷰 볼륨이 커지면 비동기 처리 재검토(Sprint 7) |
| 2026-07-29 | 리뷰 집계 단위 | 학수번호(`courses.code`) 기준으로 **학기를 넘나들며** 리뷰·평점·해시태그를 합산 | `courses`가 학기(2026-1/2026-2)마다 별도 row라서, row 단위로만 집계하면 학기가 바뀔 때마다 리뷰가 0으로 리셋된 것처럼 보임. `code`가 없는 소수 row는 과목명으로 대체 매칭(`lib/db/queries.ts:getSiblingCourseIds`). 리뷰 자체는 사용자가 보던 정확한 `course.id`에 저장. |
| 2026-07-29 | 홈 "인기 과목" 정렬 기준 | 당분간 `enrolledCount`(수강인원) 유지 | 리뷰 볼륨이 아직 없어 평점 기반 정렬은 무의미. 실사용 리뷰가 쌓이면 재검토(1.5). |
| 2026-07-29 | 학문분야 분류 체계 | 대분류 10개 · 소분류 66개(자체 구성) | 표준 학문분류표(KECD 등)를 그대로 쓰지 않고, 이 대학 실제 147개 개설학과 이름을 근거로 직접 구성(`lib/db/scripts/seed-field-tags.ts`). 필요하면 이후 스프린트에서 세분화 가능. |
| 2026-07-29 | 분야 태깅 근거 | 과목명만 사용 (강의계획서 미사용) | 원본 데이터에 강의계획서 본문이 없음(Sprint 0). 정확도 한계가 있을 수 있음 — 강의계획서 확보 시 재분류 고려. |
| 2026-07-29 | 임베딩 모델 | OpenAI `text-embedding-3-small` (1536차원) | 저렴하고 한국어 짧은 텍스트에 충분한 성능. pgvector 0.8.1을 Neon에 활성화(`enable-pgvector.ts`). |
| 2026-07-29 | 산업분야 연관도 임계값 | 코사인 유사도 ≥ 0.4, 태그당 상위 40개 후보 | 0.3에서는 오탐(무관한 과목)이 섞여 올림. 여전히 완벽하지 않아 사람이 최종적으로 몇 건 걸러냈다(4.3). |
| 2026-07-29 | 내 전공 vs 타 전공 비교 기준 | `courses.department`(개설학과) 직접 비교 | `course_department_tracks.department_label`은 원문 자유 텍스트라 학과명과 정확히 일치하지 않는 경우가 많아(예: "기계설계(나노바이오)" vs "나노바이오기계시스템") 더 불안정하다고 판단. 더 정교한 매칭은 필요성이 확인되면 나중에 재검토. |
| 2026-07-29 | 학과 졸업요건 데이터 확보 방식 | **더미 데이터** (전자공학부·컴퓨터인공지능학부 2024학번 2개) | PRD 8.4가 허용. 전공필수 목록만 실제 데이터, 학점 요건 수치는 통상적인 값으로 채운 더미. 학교 협의는 미착수. |
| 2026-07-29 | 선수과목 데이터 | **더미 데이터** (두 학과 6개 관계) | 공식 교육과정표 미확보. F4 UI에서 "예시/참고용" 명시 필요(Sprint 6에서 반영). |
| 2026-07-30 | 교양 과목 데이터 소스 | **해결** — 사용자가 `course/2026_1·2학기_교양.xlsx` 제공 | Sprint 3.5 완료. 학부전공 파일과 컬럼 구조 동일해 `import-courses.ts` 파서 변경 없이 `SOURCES`에 추가만 했다. |
| 2026-07-30 | 회원 시스템 도입 여부 | 익명 전용 → 학번+이메일 인증+비밀번호 실계정 | PRD의 "개인정보 최소 수집" 전제에서 벗어나는 결정. 사용자 요청으로 진행, PRD는 안 고치고 여기 기록(Sprint 8.1) — 채팅 기록에만 있던 걸 문서화. |
| 2026-07-30 | F5 지도 API 벤더 | 카카오맵 (JS SDK + 로컬 API) | 네이버지도 대안도 검토했으나 국내 로컬 API(건물명 → 좌표 검색) 활용도가 카카오 쪽이 이번 용도에 더 적합했다. |
| 2026-07-30 | 홈 "인기 과목" 정렬 기준 재검토 | `enrolledCount` → 최근 30일 리뷰 증가량 + 평균 평점 3.5 이상 | 1.5/4.5에서 남겨둔 "리뷰 쌓이면 재검토" 약속을 실제로 이행(Sprint 8.2). 후보가 limit보다 적으면 `enrolledCount` 순으로 남은 자리를 채우는 폴백 유지. |
| 2026-07-30 | F4 학년 제약 허용 폭 | 1개 학년 선이수까지 허용(`GRADE_LOOKAHEAD = 1`) | 완전히 못 박으면 3학년 2학기생조차 한 학기 차이인 4학년 1학기 과목을 못 받는 등 너무 빡빡했다(Sprint 8.3). |
| 2026-07-30 | 관리자 페이지 요구사항 정리 | PRD에 **13장(관리자 기능 요구사항)**을 신설해 직접 반영 | 다른 F1~F4 절과 달리 이번 13장은 "PRD는 안 고친다" 원칙의 예외 — 사용자가 관리자 페이지 설계를 위해 PRD 자체에 명시적으로 반영해달라고 요청. 구현은 Sprint 9로 계획만 세우고 아직 착수 전. |
| 2026-07-30 | 산업분야 카테고리 확장 | 6개 → 11개 | 사용자 요청. 기존 임베딩·연관도 스코어링 파이프라인 그대로 재사용, 신규 카테고리만 추가 시딩(Sprint 8.4). |
| 2026-07-30 | 이수구분 카탈로그 확장 | 4개(전공필수/전공선택/기초필수/계열공통) → 8개, 일반선택/교직/군사학 추가 | 사용자가 파일 제공. `requirementTypeEnum`에 `ALTER TYPE ADD VALUE`로 추가(마이그레이션 0004). |
| 2026-07-30 | `import-courses.ts` 재실행 시 학수번호 없는 과목 중복 삽입 버그 | (교과목명, 개설학과, 분반, 학기)로 사전 존재 확인하도록 수정 | Postgres 유니크 인덱스가 NULL을 서로 다르다고 취급해서 생긴 문제. 과거 재실행분까지 포함해 누적된 중복 2341건을 정리(리뷰 유실 없음, Sprint 8.7 참고). |
| 2026-07-30 | 군사학/일반선택 학과 결측 처리 | 군사학은 "교무처 학사지원과"로 일괄 백필, 일반선택은 결측 row 스킵 | 군사학은 원본에 학과가 있는 row 전부 같은 값이라 안전하게 추론 가능했지만, 일반선택은 학과가 다양해 추론 불가 — 억지로 채우지 않고 스킵(Sprint 8.7). |
| 2026-07-29 | Vercel 프로젝트 연결 | 팀 `tarae0419s-projects`에 `zip` 프로젝트 생성, GitHub(`Tarae0419/zip`) 연동 | 계정에 있던 기존 `curator` 프로젝트는 이 앱과 무관해 건드리지 않음. `DATABASE_URL`/`OPEN_AI_API_KEY`를 production/preview/development 세 환경에 등록. |
| 2026-07-29 | pnpm 설정 위치 | `package.json`의 `pnpm.overrides` → `pnpm-workspace.yaml`의 `overrides:`로 이동 | 첫 Vercel 배포가 `ERR_PNPM_LOCKFILE_CONFIG_MISMATCH`로 실패해서 발견. 최신 pnpm은 `package.json`의 `pnpm` 필드를 더 이상 읽지 않는다(세션 내내 뜬 경고의 원인이었음). |
| 2026-07-29 | `main` 푸시 = 프로덕션 배포 | 기존 동작 그대로 유지, 문서화만 함 | Vercel Git 연동에서 `main`이 프로덕션 브랜치로 인식돼, 별도 `--prod` 없이도 `main` 푸시마다 자동으로 프로덕션에 배포되고 있었다는 걸 Sprint 7에서 확인. 프리뷰 단계 없이 바로 프로덕션에 나간다는 뜻 — 7.1(Neon 프리뷰 브랜치)과 별개로 feature-branch/PR 워크플로 도입을 고려할 만하지만 지금은 결정하지 않음. |
| 2026-07-29 | middleware → proxy 마이그레이션 | `middleware.ts` → `proxy.ts`, `export function middleware` → `export function proxy` | Next.js 16이 `middleware` 컨벤션을 deprecated 처리(`proxy`로 개명). `next build` 경고로 발견, 공식 문서 확인 후 수동 마이그레이션(파일명·함수명만 변경, `config`/matcher는 동일). |
| 2026-07-29 | Vercel Observability Plus | 미활성화, 이번 스프린트에서는 활성화하지 않음 | `vercel-optimize` 스킬이 라우트별 실측 지표를 요구했으나 이 팀에 비활성화돼 있어(`no_oplus_probe`) 스캐너 전용(코드 패턴만) 모드로 대체 — 사용자에게 확인 후 결정. 유료 기능이라 활성화 여부는 추후 필요성이 커지면 재검토. |
