# 수강길잡이 개발 스프린트 플랜

| 항목 | 내용 |
| --- | --- |
| 기준 문서 | `docs/PRD.md` (v1.1), `CLAUDE.md` |
| 작성일 | 2026-07-29 |
| 최종 수정일 | 2026-07-29 |
| 상태 | Sprint 0 완료 · Sprint 1 완료 · Sprint 2 진행 대기 |

---

## 문서 관리 방법

- 작업을 끝낼 때마다 해당 체크박스를 `- [ ]` → `- [x]`로 바꾼다. 완료 조건(DoD)을 모두 만족했을 때만 체크한다 — 코드가 있다고 체크하지 말고, 각 항목 하단의 **DoD**를 실제로 충족했는지 확인한다.
- 스프린트를 통째로 끝내면 위 표의 **상태** 줄과 아래 **진행 현황 요약**의 상태 컬럼을 함께 갱신하고, **최종 수정일**을 갱신한다.
- 스프린트는 순서대로 진행하는 것을 기본으로 한다. 앞 스프린트의 선행조건이 끝나지 않았는데 뒤 스프린트를 먼저 시작해야 하면, 그 사실과 이유를 해당 스프린트의 "메모" 항목에 남긴다.
- 진행 중 PRD에 없던 결정(예: 데이터 소스 변경, 범위 축소)을 내리면 하단 **오픈 이슈 / 의사결정 로그**에 날짜와 함께 기록한다. PRD 자체는 별도 합의 없이 고치지 않는다.
- 새 작업이 필요해지면 해당 스프린트 안에 항목을 추가하고, 기존 번호 체계를 건드리지 않도록 하위 항목(예: `1.4.1`)으로 끼워 넣는다.

---

## 진행 현황 요약

| Sprint | 범위 | PRD 매핑 | 상태 |
| --- | --- | --- | --- |
| 0 | 기반 인프라 (DB/배포/에이전트) | 10장 | ✅ 완료 |
| 1 | 익명 사용자 식별 & 리뷰 작성 | F1 쓰기 경로 | ✅ 완료 |
| 2 | AI 해시태그 추천 & 리뷰 요약 | F1 AI 경로 | ⬜ 대기 (OpenAI 키 필요) |
| 3 | 분야 통합 검색 마무리 | F2 | ⬜ 대기 |
| 4 | 산업/진로 분야 검색 | F3 | ⬜ 대기 |
| 5 | 커리큘럼 데이터 확보 | F4 기반 | ⬜ 대기 |
| 6 | AI 커리큘럼 추천 엔진 | F4 | ⬜ 대기 |
| 7 | 배포/운영 정비 & QA | 전체 | ⬜ 대기 |

PRD 11장 로드맵(Phase1=F1+F2, Phase2=F3, Phase3=F4)을 그대로 따르되, 실제 구현 단위(쓰기 경로/AI 경로/데이터 확보를 분리)로 더 잘게 쪼갰다.

---

## Sprint 0 — 기반 인프라 (완료)

**목표**: 이후 모든 스프린트가 딛고 설 인프라를 갖춘다.

- [x] 기술 스택 확정 및 아키텍처 문서화 (`docs/PRD.md` 10장)
- [x] Neon Postgres + Drizzle ORM 연결, `lib/db/schema.ts` 10개 테이블 설계·마이그레이션
- [x] 실제 개설 교과목 데이터 적재 (2026-1/2학기, 5,275건) — `lib/db/scripts/import-courses.ts`
- [x] Vercel 공식 스킬(`deploy-to-vercel` 등) + 프로젝트 전용 에이전트(`nextjs-frontend`/`neon-db`/`ai-integration`/`vercel-deploy`) 세팅
- [x] `CLAUDE.md` 작성
- [x] 홈/검색/과목상세 화면을 mock에서 실 DB 쿼리로 전환 (`lib/db/queries.ts`)

**메모**: F3(분야탐색)·F4(커리큘럼)는 아직 `lib/mock-data.ts`를 그대로 쓴다 — 각각 Sprint 4, 6에서 실 DB로 전환한다.

---

## Sprint 1 — 익명 사용자 식별 & 리뷰 작성 (F1 쓰기 경로)

**목표**: 지금은 조회만 되는 F1을 "쓰기"까지 완성해 실제 리뷰가 쌓이기 시작하게 한다. Sprint 2(AI 요약)와 Sprint 3(분야 태깅)이 여기서 나온 데이터에 의존한다.

**선행조건**: Sprint 0.

**작업 항목**

- [x] 1.1 익명 식별자 발급 — 로그인 없이 브라우저별 `anonId`를 쿠키로 발급하는 유틸/미들웨어 작성 (`users.anon_id`와 매칭)
  - DoD: 최초 방문 시 `users` 테이블에 anon 레코드가 생성되고, 재방문 시 동일 쿠키로 동일 레코드를 재사용한다.
  - 구현: `middleware.ts`(모든 요청에 `sgz_anon_id` httpOnly 쿠키 발급, 2년 유지) + `lib/auth/anon-user.ts`(`getAnonId`/`ensureAnonUser`, 리뷰 제출 시점에 upsert). curl로 `Set-Cookie` 발급 확인.
- [x] 1.2 리뷰 작성 Server Action — `components/review-composer.tsx`의 "실제 저장 로직 없음" 상태를 제거하고 `reviews` 테이블에 실제 insert
  - DoD: 별점(1~5)·본문·해시태그 다중선택이 저장되고, 과목 상세 페이지 새로고침 시 목록에 반영된다. (PRD 8.1 요구사항 1)
  - 구현: `lib/actions/reviews.ts`의 `submitReview`. `ReviewComposer`에 `courseId` prop 추가, 실제 서버 액션 호출 + 로딩/에러 상태로 교체.
  - **설계 메모**: `courses`는 학기·분반마다 별도 row(코드+분반+학기 유니크)라서, 리뷰를 정확히 보고 있던 `course.id`에 저장하되 **조회 시 같은 학수번호(code)의 모든 학기 row를 묶어 집계**하도록 `lib/db/queries.ts:getCourseView`를 수정했다(그렇지 않으면 학기가 바뀔 때마다 리뷰가 리셋된 것처럼 보임). 자세한 내용은 오픈 이슈 로그 참고.
- [x] 1.3 어뷰징 방지 최소 로직 — 동일 `anonId`의 동일 과목 단시간 반복 등록, 극단적 평점 연속 등록 등을 `is_filtered`로 걸러내는 최소 규칙
  - DoD: 반복 등록 시나리오를 재현했을 때 두 번째 리뷰부터 `is_filtered = true`로 저장되고 집계(평점/해시태그 %)에서 제외된다. (PRD 8.1 요구사항 7)
  - 구현: `lib/actions/reviews.ts:isRepeatSubmission` — 동일 anonId가 같은 과목(학수번호 기준)에 이미 리뷰가 있으면 이후 제출은 `is_filtered=true`로 저장(삭제하지 않고 감사 목적으로 보관). 실 DB에 테스트 데이터를 넣어 1번째 `isFiltered=false`, 2번째 `isFiltered=true`, 집계에서 2번째가 제외됨을 확인 후 정리.
  - 범위 밖으로 남긴 것: 극단적 평점(별점 테러) 자체를 탐지하는 통계 기반 로직은 아직 없음 — 리뷰 볼륨이 쌓인 뒤 Sprint 2~3 사이에 재검토.
- [x] 1.4 과목 상세 페이지 캐시/재검증 — 리뷰 작성 직후 `getCourseView` 결과가 갱신되도록 revalidate 처리
  - DoD: 리뷰 등록 후 페이지 새로고침 없이(또는 즉시 새로고침으로) 리뷰 수·평점이 바뀐다.
  - 구현: `submitReview`에서 `revalidatePath("/courses/[id]")` + `revalidatePath("/")` 호출, 클라이언트에서 성공 시 `router.refresh()`.
- [x] 1.5 홈 "인기 과목" 로직 재검토 — 리뷰가 실제로 쌓이기 시작하면 `enrolledCount` 대신/함께 리뷰 수·평점을 반영할지 결정
  - DoD: 결정 내용을 오픈 이슈 로그에 기록하고, 필요 시 `getPopularCourses` 정렬 기준 수정.
  - 결정: 이번 스프린트에서는 그대로 `enrolledCount` 순 유지 (아래 오픈 이슈 로그 참고). 실사용 리뷰가 충분히 쌓이면 재검토.

**메모(다음 세션에서 브라우저로 직접 확인할 것)**: DB insert·중복 필터링·집계 로직은 Neon에 테스트 리뷰를 직접 넣고 지운 뒤 검증했고, 미들웨어 쿠키 발급과 페이지 렌더링은 curl로 확인했다. 다만 이 세션에는 브라우저 자동화 도구가 없어 **모달 클릭 → 별점 선택 → 등록 버튼까지 실제 브라우저 인터랙션은 아직 육안으로 확인 못 했다** — 다음 작업 시작 전에 한 번 실제로 클릭해보는 걸 권장.

**담당 에이전트**: `nextjs-frontend`(폼/Server Action UI), `neon-db`(어뷰징 규칙·인덱스)

---

## Sprint 2 — AI 해시태그 추천 & 리뷰 요약 (F1 AI 경로)

**목표**: F1을 PRD 완료 조건까지 끝낸다.

**선행조건**: Sprint 1 (리뷰가 실제로 쌓여야 요약 대상이 생김). LLM 벤더 결정 필요(아래 오픈 이슈 참고).

**작업 항목**

- [ ] 2.1 LLM API 벤더 결정 — 오픈 이슈 로그에 결정 기록, `ai-integration` 에이전트에 반영
- [ ] 2.2 자유 텍스트 → 해시태그 추천 — 리뷰 작성 중 입력한 본문을 바탕으로 사전 정의 태그 후보를 추천하는 API
  - DoD: `review-composer.tsx`에서 본문 입력 후 AI 추천 태그가 실제로 채워지고 사용자가 채택/수정할 수 있다. (PRD 8.1 요구사항 2)
- [ ] 2.3 과목별 AI 요약 생성 — 리뷰 5개 이상인 과목을 대상으로 3~5문장 요약(전반 평가·장단점·추천 대상 포함) 생성 후 `summaries` 테이블에 캐싱
  - DoD: 리뷰 5개 이상 과목 상세 페이지에서 실제 생성된 요약이 노출되고, `app/courses/[id]/page.tsx`의 "AI 요약을 준비하고 있어요" 상태가 정상 동작으로 대체된다.
- [ ] 2.4 요약 재생성 트리거 — 신규 리뷰가 일정 수 누적되면 요약을 자동 재생성
  - DoD: 재생성 기준(예: N개 누적)을 코드/문서에 명시하고, 기준 충족 시 `summaries.generated_at`이 갱신됨을 확인한다.
- [ ] 2.5 극단적 리뷰 처리 — 평점이 크게 갈리는 과목은 요약에 "호불호가 갈리는 강의"를 명시하도록 프롬프트 반영 (PRD 8.1 Edge Case)
  - DoD: 의도적으로 평점이 양극화된 테스트 데이터로 생성했을 때 요약 문구에 반영됨을 확인한다.

**담당 에이전트**: `ai-integration`

---

## Sprint 3 — 분야 통합 검색 마무리 (F2)

**목표**: 지금 "분야 일치" 검색은 `field_tags`가 비어 있어 항상 빈 결과다. 실제 태그 데이터를 채워 F2를 PRD 완료 조건까지 끝낸다.

**선행조건**: Sprint 0(courses 데이터). Sprint 2와 병행 가능(서로 독립).

**작업 항목**

- [ ] 3.1 학문분야 분류 체계 정의 — 대분류-소분류 체계 설계 (PRD 8.2 요구사항 2), `field_tags.parent_id`로 표현
- [ ] 3.2 동의어 사전 시딩 — `field_tags.synonyms`에 "수학"↔"수리과학" 같은 동의어 데이터 입력 (PRD 8.2 요구사항 6)
- [ ] 3.3 과목-분야 AI 1차 분류 — 과목명(+ 확보 가능하면 강의계획서)을 기반으로 `course_field_tags` 자동 생성
  - DoD: 최소 1개 이상 학과의 과목이 실제로 분야 태깅되고, `/search?q=수학` 같은 쿼리에서 "분야 일치" 섹션에 실제 결과가 뜬다.
- [ ] 3.4 담당자 검수 플로우 — AI 1차 분류 결과를 사람이 확인/수정할 수 있는 최소한의 관리 화면 또는 스크립트
  - DoD: 잘못 태깅된 항목을 수정하는 절차가 문서화되어 있다.
- [ ] 3.5 교양 과목 데이터 확보 — 현재 DB에는 학부전공 과목만 있고 교양 과목이 없다 (CLAUDE.md "Data notes" 참고). 별도 교양 개설과목 파일을 확보해 `courses`에 추가 적재
  - DoD: `requirement_type = '교양'` 행이 DB에 존재하고 검색/필터에서 정상 노출된다.

**담당 에이전트**: `ai-integration`(분류), `neon-db`(시딩/적재)

---

## Sprint 4 — 산업/진로 분야 검색 (F3)

**목표**: PRD Phase 2. `/fields` 페이지를 mock에서 실 DB로 전환한다.

**선행조건**: Sprint 3의 분야 태깅 파이프라인 경험을 재사용.

**작업 항목**

- [ ] 4.1 산업/진로 태그셋 정의 — 반도체, AI·데이터사이언스 등 초기 카테고리를 `industry_tags`에 시딩 (PRD 8.3 요구사항 1)
- [ ] 4.2 연관도 스코어링 파이프라인 — 과목 설명/키워드 임베딩 + pgvector 유사도로 `course_industry_tags.relevance_score` 계산 (PRD 10.3)
  - DoD: 임의 산업 분야 하나에 대해 연관도 상위 과목 목록이 실제로 채워진다.
- [ ] 4.3 담당자 검수 반영 — 3.4와 동일한 방식으로 산업 태그 결과 검수
- [ ] 4.4 `/fields` 페이지 실 DB 전환 — `components/fields-explorer.tsx`의 `fieldCategories`/mock 의존 제거, `lib/db/queries.ts`에 `getIndustryFieldsWithCourses` 등 추가
  - DoD: 화면에서 카테고리별 과목 수·목록이 실제 DB 값과 일치한다.
- [ ] 4.5 내 전공 vs 타 전공 구분 — `course_department_tracks`를 이용해 로그인(익명) 사용자의 학과와 비교해 "내 전공 과목"/"타 전공 과목" 라벨 표시 (PRD 8.3 요구사항 5)
  - DoD: 사용자의 학과 정보가 있을 때 라벨이 정확히 구분되고, 없을 때는 라벨을 생략한다.

**담당 에이전트**: `ai-integration`, `nextjs-frontend`

---

## Sprint 5 — 커리큘럼 데이터 확보 (F4 기반)

**목표**: F4는 4개 기능 중 복잡도가 가장 높고, 무엇보다 **데이터가 없으면 시작할 수 없다** (PRD 11장). 로직을 짜기 전에 데이터부터 확보한다.

**선행조건**: 없음(다른 스프린트와 독립적으로 먼저 시작 가능하지만, 데이터 확보에 시간이 걸릴 수 있어 최대한 일찍 착수 권장).

**작업 항목**

- [ ] 5.1 학과 졸업요건 데이터 확보 방안 결정 — 학교 협의(교무처·학과 사무실) 또는 PRD 8.4에 명시된 더미 데이터 삽입 중 선택
  - DoD: 결정 내용과 근거를 오픈 이슈 로그에 기록.
- [ ] 5.2 `curricula` 테이블 시딩 — 학과별 전공필수 목록, 전공선택 최소학점, 교양 요건, 총 이수학점을 최소 1개 학과 기준으로 입력
  - DoD: 최소 1개 (학과, 입학년도) 조합에 대해 조회 가능한 실데이터가 있다.
- [ ] 5.3 선수과목 관계 데이터 확보 — 현재 `courses.prerequisite_codes`는 항상 비어 있음(원본 엑셀에 없음). 별도 소스 확보 또는 수동 입력 범위 결정
  - DoD: 최소 1개 학과 커리큘럼에 대해 선수과목 순서가 실제로 채워진다. 확보 불가 시 "F4는 선수과목 순서를 반영하지 않는다"를 명시적 제약으로 문서화.
- [ ] 5.4 커리큘럼 버전 관리 확인 — (학과, 입학년도) 유니크 제약이 실제 다년도 데이터로 문제없이 동작하는지 검증

**담당 에이전트**: `neon-db`

---

## Sprint 6 — AI 커리큘럼 추천 엔진 (F4)

**목표**: PRD Phase 3. `/curriculum`을 mock에서 실 추천 로직으로 전환한다.

**선행조건**: Sprint 5(커리큘럼 데이터), Sprint 4(관심분야 연관도 데이터), Sprint 1(사용자 기이수 과목 입력 경로).

**작업 항목**

- [ ] 6.1 입력 폼 실데이터 연동 — 학과(복수전공 포함)·현재 학년/학기·기이수 과목·관심분야·잔여 학기 입력을 `users` 테이블에 저장 (PRD 8.4 입력 1~4)
- [ ] 6.2 결정론적 학점 계산 로직 — 미이수 전공필수 우선 배치, 전공선택 잔여 학점 계산 (PRD 8.4 추천로직 6~7). **AI 위임 대상이 아님** — `ai-integration`이 아닌 일반 백엔드 로직으로 구현
  - DoD: 임의 사용자 입력에 대해 전공필수 미이수 과목이 우선 배치된 학기별 결과가 나온다.
- [ ] 6.3 관심분야 매칭 추천 (AI 위임 부분) — 전공선택 잔여·자유선택·교양 학점에 대해 관심분야 연관도가 높은 과목을 추천하고 사유 생성 (PRD 8.4 추천로직 8)
- [ ] 6.4 인터랙티브 재계산 — 사용자가 과목을 제외/추가하면 나머지 추천이 재계산 (PRD 8.4 요구사항 12)
  - DoD: `components/curriculum-planner.tsx`의 `setTimeout` 목업 로직이 실제 서버 재계산 호출로 대체된다.
- [ ] 6.5 서버리스 타임아웃 대응 — 연산이 길어질 경우 스트리밍 응답 또는 상태 폴링 구조 적용 (PRD 10.4)
- [ ] 6.6 참고용 안내 문구 — "최종 확인은 학과 사무실을 통해주세요" 문구가 실제 결과 화면에도 노출되는지 확인 (PRD 8.4 완료조건)
- [ ] 6.7 복수전공/부전공, 고학년 엣지케이스 처리 (PRD 8.4 Edge Case)

**담당 에이전트**: `ai-integration`(추천), `nextjs-frontend`(UI), `neon-db`(학점 계산 쿼리)

---

## Sprint 7 — 배포/운영 정비 & QA

**목표**: 실사용 가능한 수준으로 다듬는다. 특정 Phase에 종속되지 않으므로 앞 스프린트들과 일부 병행 가능.

**작업 항목**

- [ ] 7.1 Neon 브랜치 × Vercel 프리뷰 연동 — `.claude/skills/neon-branch-preview-sync/SKILL.md` 절차를 실제로 Vercel 대시보드에서 설정
- [ ] 7.2 접근성/UX 감사 — `web-design-guidelines` 스킬로 주요 화면(리뷰 작성 폼, 검색 필터, 커리큘럼 플래너) 점검 및 수정
- [ ] 7.3 Vercel 비용/성능 점검 — `vercel-optimize` 스킬로 F4 등 무거운 라우트 점검
- [ ] 7.4 프로덕션 배포 — `deploy-to-vercel`/`vercel-cli-with-tokens` 스킬로 첫 프로덕션 배포 (사용자 명시적 승인 필요)
- [ ] 7.5 최소 테스트 체계 도입 — 현재 테스트 스위트 없음(CLAUDE.md 참고). 핵심 쿼리(`lib/db/queries.ts`)와 학점 계산 로직(6.2) 최소 단위 테스트 추가

---

## 오픈 이슈 / 의사결정 로그

> PRD 10.4는 ORM·LLM 벤더 같은 미결정 사항을 "14장 오픈 이슈"에서 관리한다고 적혀 있지만, 실제 `docs/PRD.md`에는 14장이 존재하지 않는다(12장에서 끝남). PRD를 임의로 고치는 대신, 이 문서에서 대신 추적한다.

| 날짜 | 이슈 | 결정 | 비고 |
| --- | --- | --- | --- |
| 2026-07-29 | ORM 선택 | **Drizzle** | Neon 서버리스 드라이버와의 통합, 콜드스타트/번들 크기 고려 |
| 2026-07-29 | LLM API 벤더 | **OpenAI** | Sprint 2(해시태그 추천·AI 요약) 착수 전 `OPENAI_API_KEY` 환경변수 필요 — 발급 시 요청 예정, `.env.local`에 추가하고 커밋 금지 |
| 2026-07-29 | 리뷰 집계 단위 | 학수번호(`courses.code`) 기준으로 **학기를 넘나들며** 리뷰·평점·해시태그를 합산 | `courses`가 학기(2026-1/2026-2)마다 별도 row라서, row 단위로만 집계하면 학기가 바뀔 때마다 리뷰가 0으로 리셋된 것처럼 보임. `code`가 없는 소수 row는 과목명으로 대체 매칭(`lib/db/queries.ts:getSiblingCourseIds`). 리뷰 자체는 사용자가 보던 정확한 `course.id`에 저장. |
| 2026-07-29 | 홈 "인기 과목" 정렬 기준 | 당분간 `enrolledCount`(수강인원) 유지 | 리뷰 볼륨이 아직 없어 평점 기반 정렬은 무의미. 실사용 리뷰가 쌓이면 재검토(1.5). |
| — | 학과 졸업요건 데이터 확보 방식 | 미정 | Sprint 5.1에서 결정 |
| — | 선수과목 데이터 확보 가능 여부 | 미정 | Sprint 5.3에서 결정, 불가 시 F4 제약사항으로 문서화 |
| — | 교양 과목 데이터 소스 | 미정 | Sprint 3.5에서 확보 |
